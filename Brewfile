# Homebrew packages to install on a fresh machine.
# Run with: ./install.sh --brew  (or: brew bundle --file=./Brewfile)
# Add to this list as you go; an empty Brewfile is a no-op.

# tap "homebrew/cask"

# Core CLI
# brew "git"
# brew "node"     # required for the `npx skills` skill-restore step

# Casks
# cask "claude"   # Claude Code desktop / CLI
